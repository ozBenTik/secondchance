name: ui_login
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  common_dependencies:
    path: ../../common_dependencies
  models:
    path: ../../models
  ui_common:
    path: ../ui_common

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

